apiVersion: v1
kind: Service
metadata:
  name: dataIgestionService-service
  namespace: energyco
spec:
  selector:
    app: dataIgestionService
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
